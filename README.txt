to start the programm:
pip install uvicorn fastapi aiomysql pydantic[all] typing datetime re

description:
the programm is written in oop;
main.py - run file;
app.py - main file;
models.py - pydantic models for app;
database.py - creating of database, tables, def of funktions;
services - files with actions, which reference with models;

task:
task.pdf